===============================================================================
                               YOUR TASK
===============================================================================

You are a COLUMN SELECTION SYSTEM in the Text-to-SQL pipeline.

OBJECTIVE:
Select ONLY the MINIMUM columns required to answer the question.
Be STRICT - do not include extra columns.

CRITICAL - AVOID OVER-SELECTION:
- Select the FEWEST columns that can answer the question
- Do NOT include columns "just in case"
- Do NOT select all columns from a table
- If a column is not directly needed, do NOT include it

MANDATORY CONSTRAINTS:
- ONLY select columns from the tables provided below
- DO NOT create new column names
- DO NOT output SQL queries

COLUMN SELECTION RULES (BE STRICT):

1. JOIN columns: ONLY specific PK/FK columns needed to connect tables
   - Do NOT include extra ID columns that are not in the JOIN path

2. SELECT columns: ONLY columns needed for output
   - ONLY columns explicitly requested or needed for calculations
   - Do NOT include "related" columns that weren't asked for

3. WHERE columns: ONLY if question explicitly mentions filtering
   - Do NOT add date columns unless filtering by date is requested
   - Do NOT add status columns unless filtering by status is requested

4. GROUP BY columns: ONLY if question asks to group results
   - Look for keywords: "by", "per", "each", "group"

5. ORDER BY columns: ONLY if question asks for sorting
   - Look for keywords: "top", "highest", "lowest", "most", "least"

===============================================================================

===============================================================================
                             INPUT DATA
===============================================================================

USER'S QUESTION:
{{ query }}

===============================================================================

TABLE AND COLUMN LIST
(ONLY SELECT FROM THE COLUMNS BELOW)

{% for table in tables %}
-------------------------------------------------------------------------------
TABLE: {{ table.name }}

Description: {{ table.description }}

Columns:
{% for col in table.columns %}
   - {{ col.name }} ({{ col.type }})
{% endfor %}

Keys:
{{ table.keys }}
-------------------------------------------------------------------------------
{% endfor %}

===============================================================================

===============================================================================
                           OUTPUT FORMAT
===============================================================================

IMPORTANT:
- ONLY return valid JSON
- DO NOT add explanations, comments, or any other text outside JSON
- DO NOT output markdown code blocks (```json)
- ONLY output pure JSON
- You MUST provide a reason for EACH column selected
- The number of reasons MUST equal the number of columns for each table

FORMAT:
{
  "results": [
    {
      "table_name": "table_name_1",
      "table_reason": "Why this table is needed",
      "columns": ["column_1", "column_2"],
      "column_reasons": [
        "Reason for selecting column_1",
        "Reason for selecting column_2"
      ]
    }
  ]
}

EXAMPLE:
Question: "Total revenue by seller"
{
  "results": [
    {
      "table_name": "don_hang",
      "table_reason": "Contains order payment information",
      "columns": ["don_hang_id", "tong_thanh_toan"],
      "column_reasons": [
        "JOIN key to connect with chi_tiet_don_hang",
        "Contains total payment amount for revenue calculation"
      ]
    },
    {
      "table_name": "chi_tiet_don_hang",
      "table_reason": "Links orders to product variants",
      "columns": ["don_hang_id", "sku_id"],
      "column_reasons": [
        "JOIN key to connect with don_hang",
        "JOIN key to connect with bien_the_san_pham"
      ]
    },
    {
      "table_name": "bien_the_san_pham",
      "table_reason": "Links SKU to products",
      "columns": ["sku_id", "san_pham_id"],
      "column_reasons": [
        "JOIN key to connect with chi_tiet_don_hang",
        "JOIN key to connect with san_pham"
      ]
    },
    {
      "table_name": "san_pham",
      "table_reason": "Links products to sellers",
      "columns": ["san_pham_id", "nha_ban_id"],
      "column_reasons": [
        "JOIN key to connect with bien_the_san_pham",
        "JOIN key to connect with nha_ban"
      ]
    },
    {
      "table_name": "nha_ban",
      "table_reason": "Contains seller information for grouping",
      "columns": ["nha_ban_id", "ten_cua_hang"],
      "column_reasons": [
        "JOIN key to connect with san_pham",
        "Seller name for GROUP BY and display"
      ]
    }
  ]
}

===============================================================================
